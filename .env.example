# Create a free Redis database in Upstash, then copy its REST credentials here.
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-standard-token

# Optional: change this if multiple sites share the same Redis database.
VIEW_COUNT_KEY=bio-website:views
